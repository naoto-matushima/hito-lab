import { Container } from "@/components/ui";

/**
 * TOPページの本実装はPhase 4（TOP / Theme / Industry Hub）で行う。
 * ここではHeader/Footer/Design Tokenが機能していることを確認するための最小プレースホルダ。
 */
export default function Home() {
  return (
    <Container className="py-24 text-center">
      <h1>人手不足研究所</h1>
      <p className="mt-4 text-text-secondary">TOPページはPhase 4（TOP / Theme / Industry Hub）で実装予定です。</p>
    </Container>
  );
}
